install in system path

pip install pyautogui
pip install keyboard

Macro will work only in WINDOWED mode at a resolution of 1280x720
Window must be in the down left corner of the screen

Line 7 to change macro keybind
	if keybind is not on the keypad comment line 37 and 38 (just put # at the start of each line)
	
Line 14 to change open map keybind